# Aerius API
A high-performance weather forecasting engine. To run the application, execute the following command: `npm install && node src/index.js`
## Dependencies
- express: ^4.17.1
- body-parser: ^1.20.0